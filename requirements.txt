# Core bot framework
# Do not install the unrelated package named telegram; it breaks imports.
python-telegram-bot==21.3

# Browser automation
selenium==4.21.0

# WebDriver management (auto-downloads matching chromedriver)
webdriver-manager==4.0.1
